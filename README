
CW SAMPLE PROJECT

1、 先进行信号预处理；

2、 某种采样算法
定时采样法，采样速率为59.88Hz；
设定定时器16.699ms，定时器到来时采样。

3、 先找到间隔码，按如下顺序：
a)、 找  7t  字间,切分出字。
b)、找 3t 字符间隔， 切分出字的字符。
c)、 找 1t 滴答间隔， 切分出字符的编码。





代码使用说明：
   cws -v [样式] 指定输出; 默认按16进制输出
            1    16 进制
			2    level_stream 按二进制输出

   示例命令：
      cws -v
	  cws -v 2


   cws -n x     传输时机设置，当采样满足 x * 采样速率 个采样点后，进行传输。
